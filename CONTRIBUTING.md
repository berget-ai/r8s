# Contributing to r8s

Thanks for your interest in contributing to r8s! This document describes how to
set up your development environment, our conventions, and what to expect from
the review process.

## Ways to Contribute

- **Report bugs** — open an issue using the bug report template
- **Suggest features** — open an issue using the feature request template
- **Improve documentation** — the docs site lives in `docs/`
- **Add components** — new operator-backed packages are always welcome
- **Fix bugs** — look for issues labeled `good first issue`

## Development Setup

**Requirements:** Node.js 20+ and npm 10+.

```bash
git clone https://github.com/berget-ai/r8s.git
cd r8s
npm install
npm run build   # build all packages
npm test        # run all tests
```

## Repository Structure

```
r8s/
├── packages/        # Publishable npm packages (@r8s/*)
│   ├── core/        # JSX factory, renderer, context, validation
│   ├── k8s-types/   # TypeScript types for Kubernetes resources
│   ├── cli/         # The `r8s` command-line tool
│   ├── recipes/     # High-level components (App, Database, Endpoint, ...)
│   └── ...          # One package per operator integration
├── examples/        # Runnable example projects
├── docs/            # r8s.berget.ai documentation site
└── k8s/             # This repo's own infrastructure (dogfooding)
```

## Making Changes

1. **Fork and branch.** Create a branch from `main`:
   `git checkout -b feat/my-change` or `fix/my-change`.

2. **Write tests.** Every package has tests in `__tests__/` using Vitest.
   New components must include tests covering rendering and operator
   declarations.

3. **Follow the conventions.**
   - Code is formatted with Prettier — run `npm run format` before committing.
   - Commits follow [Conventional Commits](https://www.conventionalcommits.org/):
     `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:`.
     Scope optional, e.g. `feat(recipes): add Kafka component`.

4. **Open a pull request.** Fill in the PR template. PRs are reviewed within
   24 hours on weekdays.

5. **CI must be green.** The CI workflow builds all packages (which also
   typechecks them) and runs all tests. A formatting check will be added
   once the Prettier config is aligned with the codebase — see
   [#39](https://github.com/berget-ai/r8s/issues/39).

## Adding a New Operator Package

The full walkthrough lives in the docs:
**[r8s.berget.ai/adding-packages](https://r8s.berget.ai/adding-packages)**
(it covers new packages, new CRD components in existing packages, and new
recipes).

Short version:

1. Copy the annotated template: `cp -r packages/example packages/my-operator`
2. Replace the operator declaration and components in `src/index.ts`
3. Write tests in `__tests__/` (operator declaration, rendering, context dedup)
4. Register the package: root `tsconfig.json` (paths + references),
   `docs/scripts/generate-docs.ts` (path alias), `README.md` (packages table)
5. Open a PR — we review within 24 hours on weekdays

### Acceptance checklist

A new package is accepted when:

- [ ] Operator version is pinned as a default parameter (tested upstream release)
- [ ] `crds` array lists every CRD the components render
- [ ] Every exported prop has JSDoc (docs tables are generated from these)
- [ ] Every component has a runnable `@example` block (executed by the docs generator)
- [ ] Components check `OperatorContext` before declaring operators (deduplication)
- [ ] Tests cover: operator declaration, rendering with defaults and full props, context dedup
- [ ] `package.json` complete: `description`, `keywords`, `r8s.category`, no `"private": true`
- [ ] Registered in root `tsconfig.json` and `generate-docs.ts` aliases
- [ ] `README.md` packages table row added
- [ ] CI green: build, tests, format check

## Committing Generated Files

`k8s/rendered/manifest.yaml` is generated by CI — do not edit it by hand.
If your change affects rendered output, CI will commit the updated manifest
after merge.

## License

By contributing, you agree that your contributions will be licensed under the
[MIT License](LICENSE). Copyright is held by Berget AI AB.
