{
  "name": "@r8s/operator-cnpg",
  "version": "1.27.0",
  "description": "CloudNativePG operator for r8s: CRD components + install manifest (npm-resolved operator package)",
  "license": "MIT",
  "author": "Berget AI AB",
  "homepage": "https://r8s.berget.ai",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/berget-ai/r8s.git",
    "directory": "packages/operator-cnpg"
  },
  "bugs": "https://github.com/berget-ai/r8s/issues",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "r8s",
    "operator",
    "cnpg",
    "cloudnative-pg",
    "postgresql"
  ],
  "r8s": {
    "category": "Operators"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@r8s/core": "^0.2.0",
    "@r8s/k8s-types": "^0.2.0",
    "@r8s/crds": "^0.2.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
